apiVersion: v1
kind: Service
metadata:
  name: server1
spec:
  selector:
    app: server1
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: server2
spec:
  selector:
    app: server2
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
